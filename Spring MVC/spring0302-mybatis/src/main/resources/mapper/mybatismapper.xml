<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yuhan.mvc.mybatis.dao.IMybatisDAO">
	<select id="list" resultMap="yuhan.mvc.mybatis.dao.MybatisDAO">
		SELECT
			*
		FROM
			mabits_board
		ORDER BY
			m_id DESC
	</select>

</mapper>