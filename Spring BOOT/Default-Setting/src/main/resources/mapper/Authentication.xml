<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
						
						
<mapper namespace="com.spring.boot.mapper.Authentication">

	<select id="selectUser" resultType="com.spring.boot.DTO.AuthenticationDTO">
		SELECT
				*
		  FROM	authentication
	</select>

</mapper>